name: Build and Deploy to GKE

on:
  push:
    branches:
    - main

env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}
  GKE_CLUSTER: cluster1    
  GKE_ZONE: us-central1-c   # TODO: update to cluster zone
  DEPLOYMENT_NAME: nginx-2 # TODO: update to deployment name
  IMAGE: mio-nginx

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    # Setup gcloud CLI
    - uses: google-github-actions/setup-gcloud@master
      with:
        version: '290.0.1'
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ secrets.GKE_PROJECT }}

    # Configure Docker to use the gcloud command-line tool as a credential
    # helper for authentication
    - run: |-
        gcloud --quiet auth configure-docker

    # Get the GKE credentials so we can deploy to the cluster
    - run: |-
        gcloud container clusters get-credentials "$GKE_CLUSTER" --zone "$GKE_ZONE"

    # Build the Docker image
    - name: Build
      run: |-
        docker build \
          --tag "gcr.io/$PROJECT_ID/$IMAGE:$GITHUB_SHA" \
          --build-arg GITHUB_SHA="$GITHUB_SHA" \
          --build-arg GITHUB_REF="$GITHUB_REF" \
          .

    # Push the Docker image to Google Container Registry
    - name: Publish
      run: |-
        docker push "gcr.io/$PROJECT_ID/$IMAGE:$GITHUB_SHA"

    # Install helm
    - name: Install Helm
      run: |
        curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash
    # Deploy the Docker image to the GKE cluster
    - name: Deploy
      run: |-
  #      gcloud container clusters get-credentials cluster1 \
   #       --zone us-central1-c \
    #      --project careful-ensign-302119
     #   helm upgrade careful-ensign-302119 careful-ensign-302119-cluster/ \
        #--set randocats.image.tag=$GITHUB_SHA \
      #  --wait
        #kubectl rollout status deployment/nginx-2
        #kubectl get services -o wide	
        #./kustomize edit set image gcr.io/PROJECT_ID/IMAGE:TAG=gcr.io/$PROJECT_ID/$IMAGE:$GITHUB_SHA
        #./kustomize build . | kubectl apply -f -
        #kubectl rollout status deployment/$DEPLOYMENT_NAME
        #kubectl get services -o wide
#    - name: kubectl - Google Cloud GKE cluster.
 #     uses: ameydev/gke-kubectl-action@master
  #    env:
   #     PROJECT_ID: careful-ensign-302119
    #    APPLICATION_CREDENTIALS: ${{ secrets.GCLOUD_AUTH }}
     #   CLUSTER_NAME: cluster1
      #  ZONE_NAME: us-central1-c
	gcloud container clusters get-credentials cluster1 --zone us-central1-c --project careful-ensign-302119 && gcloud components install kubectl && kubectl -n default set image deployment/nginx-2 mi-nginx-1=gcr.io/careful-ensign-302119/github.com/sersalas/mi-nginx:latest 
